namespace CodeAcademy
{
    class Porgram
    {
        static void Main()
        {
            /*
             Vehicle class: Color (optional), Year
             Car class: inherits from Vehicle;
                        Brand, Model, FuelCapacity, FuelFor1Km, CurrentFuel (optional); 
                        ShowInfo(), Drive(float km) -> return qalan benzin
            */
        }
    }
}
